name: "Hi"

on: push

env:
  WORKFLOW_LEVEL: "THIS VALUE COMES FROM THE WORKFLOW LEVEL HAHAHAHA "

jobs:
  hello-my-actions:
    runs-on: ubuntu-latest
    name: Hello My Actions
    steps:
      - name: checkout
        uses: actions/checkout@v1
      - name: TIL README.md Generator
        uses: yongjulejule/TIL-github-action@main
  justin-yoo-action:
    runs-on: ubuntu-latest
    name: Hello action for test
    env:
      JOB_LEVEL_1: "JOB LV1 env"
    steps:
      - name: hello world
        shell: bash
        env:
          STEP_LEVEL_2: "Step LV 2 env"
        run: |
          echo "hi"
