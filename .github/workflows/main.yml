on:
  workflow_dispatch:

jobs:
  hello-my-actions:
    runs-on: ubuntu-latest
    name: Hello My Actions
    steps:
      - name: checkout
        uses: actions/checkout@v1
      - name: TIL README.md Generator
        uses: yongjulejule/TIL-github-action@test
